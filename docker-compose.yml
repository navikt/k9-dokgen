version: '3.7'
services:
  dokgen:
    build: .
    container_name: dokgen_dev_interactive
    ports:
      - "8291:8080"
    volumes:
      - ./content:/app/content
    environment:
      SPRING_PROFILES_ACTIVE: dev
